.querySelector -> method finds the 1st element of my search 
.getElementById -> method finds an element by it's id 
.innerText -> prints after the "=" 
localStorage -> obj allows you to store in the website * only stores STRING *
.setItem() -> method stores in the localhost
JSON.stringify() is used to convert the JavaScript object into a string
addEventListener -> calles an function to make the website more dynamical 






